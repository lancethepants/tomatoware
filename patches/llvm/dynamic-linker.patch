diff -Naur llvm-project-14.0.6.src.orig/clang/lib/Driver/ToolChains/Linux.cpp llvm-project-14.0.6.src/clang/lib/Driver/ToolChains/Linux.cpp
--- llvm-project-14.0.6.src.orig/clang/lib/Driver/ToolChains/Linux.cpp	2022-08-18 14:27:50.788201838 -0600
+++ llvm-project-14.0.6.src/clang/lib/Driver/ToolChains/Linux.cpp	2022-08-18 14:29:30.888163942 -0600
@@ -422,7 +422,7 @@
         Triple.getSubArch() == llvm::Triple::PPCSubArch_spe)
       ArchName = "powerpc-sf";
 
-    return "/lib/ld-musl-" + ArchName + ".so.1";
+    return "/mmc/lib/ld-musl-" + ArchName + ".so.1";
   }
 
   std::string LibDir;
@@ -448,8 +448,8 @@
         Triple.getEnvironment() == llvm::Triple::GNUEABIHF ||
         tools::arm::getARMFloatABI(*this, Args) == tools::arm::FloatABI::Hard;
 
-    LibDir = "lib";
-    Loader = HF ? "ld-linux-armhf.so.3" : "ld-linux.so.3";
+    LibDir = "mmc/lib";
+    Loader = HF ? "ld-uClibc.so.1" : "ld-uClibc.so.1";
     break;
   }
   case llvm::Triple::m68k:
@@ -462,16 +462,16 @@
   case llvm::Triple::mips64el: {
     bool IsNaN2008 = tools::mips::isNaN2008(getDriver(), Args, Triple);
 
-    LibDir = "lib" + tools::mips::getMipsABILibSuffix(Args, Triple);
+    LibDir = "mmc/lib" + tools::mips::getMipsABILibSuffix(Args, Triple);
 
     if (tools::mips::isUCLibc(Args))
-      Loader = IsNaN2008 ? "ld-uClibc-mipsn8.so.0" : "ld-uClibc.so.0";
+      Loader = IsNaN2008 ? "ld-uClibc.so.1" : "ld-uClibc.so.1";
     else if (!Triple.hasEnvironment() &&
              Triple.getVendor() == llvm::Triple::VendorType::MipsTechnologies)
       Loader =
           Triple.isLittleEndian() ? "ld-musl-mipsel.so.1" : "ld-musl-mips.so.1";
     else
-      Loader = IsNaN2008 ? "ld-linux-mipsn8.so.1" : "ld.so.1";
+      Loader = IsNaN2008 ? "ld-uClibc.so.1" : "ld-uClibc.so.1";
 
     break;
   }
